Compile code by:
"make gnu"
Run by:
"./polymer.exe"

git fetch (to see if there are changes)
git pull  (to pull from online version)

git add filename (to add a file)
git commit -m 'message' (makes your changes ready to be pushed)
git push  (to push to online version)

